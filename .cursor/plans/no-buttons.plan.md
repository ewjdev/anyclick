 # No Buttons??

If "Any click" automatically has the right context to provide actions why do we need UI buttons?

What would the version of the home page look like with no buttons and create elements with context that would provide anyclick menu with actions akin to the buttons that exist today