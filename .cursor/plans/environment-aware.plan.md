**Environment‑aware behavior**

- In `development`: default to **Cursor local agent** + optional GitHub.
- In `staging/production`: default to **GitHub issues**, AI as opt‑in.
- This keeps local dev ultra‑fast and prod very safe.
