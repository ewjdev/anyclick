{
  "name": "anyclick",
  "private": true,
  "description": "Why not click on anything? - capture DOM context, screenshots, and integrate with GitHub Issues or AI coding agents",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ewjdev/anyclick.git"
  },
  "homepage": "https://anyclick.ewj.dev",
  "bugs": {
    "url": "https://github.com/ewjdev/anyclick/issues"
  },
  "author": "ewjdev",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "prettier": "3.5.3",
    "turbo": "^2.5.6"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "resolutions": {
    "csstype": "^3.1.3"
  },
  "packageManager": "yarn@1.22.19"
}
